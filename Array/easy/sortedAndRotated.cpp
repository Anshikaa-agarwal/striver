#include<iostream>
#include<vector>
using namespace std;

bool sortedAndRotated(vector<int> nums) {
    if(nums.size()==1) return true;
    int flag = 1;

    for(int i=1; i<nums.size(); i++) {
        if(nums[i] < nums[i]) {
            
        }
    }
}